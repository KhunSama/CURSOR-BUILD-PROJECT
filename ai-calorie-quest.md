# AI Calorie Quest

Implementation follows `docs/PLAN-ai-calorie-quest.md` and the approved PRD loop:

Sign up → nutrition goals → dashboard → log meal (manual or AI) → review/confirm → macros update → XP / quests / streak / level.

Stack: Next.js + Prisma + SQLite. AI keys stay on the server. Manual logging works without an API key.
